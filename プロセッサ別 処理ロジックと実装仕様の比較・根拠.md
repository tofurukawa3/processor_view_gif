# プロセッサ別 処理ロジックと実装仕様の比較・根拠

本資料では、10x10（合計100個）のデータ行列を処理するモデルにおいて、各プロセッサアーキテクチャが採用する計算手法とその技術的根拠を定義します。

## 1. CPU (Central Processing Unit)

**処理方式: スカラ処理 (Scalar Processing)**

汎用的な処理に特化しており、フェッチ・デコード・実行のサイクルを命令ごとに繰り返します。SISD (Single Instruction, Single Data) モデルに基づきます。

* **視覚表現:** 「点」が1マスずつ移動する。
* **実装ロジック:** 2重ループによる全要素の順次走査。
* **計算量 (Theoretical Cycles):** **100**
* **主な用途:** OS制御、複雑な分岐処理。

> **📚 根拠・出典**
>
> * **Intel® 64 and IA-32 Architectures Software Developer’s Manual**: 基本的な汎用レジスタを用いたスカラ演算（ADD, MUL等）の定義。命令ポインタがシーケンシャルにメモリ上の命令を実行していく挙動に基づきます。
> * **Hennessy & Patterson, "Computer Architecture: A Quantitative Approach"**: SISDアーキテクチャおよび命令レベルの並列性（ILP）の限界に関する記述。

---

## 2. AVX2 (Advanced Vector Extensions)

**処理方式: SIMD (Single Instruction, Multiple Data)**

CPUの拡張命令セットです。256ビットのレジスタ（YMMレジスタ）を使用し、例えば32ビットの浮動小数点数（float）であれば8個を、1命令で同時に演算します。

* **視覚表現:** 「線（行）」が上から下へ移動する。
* **実装ロジック:** データ並列性を用いたベクトル演算（ループのアンローリングとベクトル化）。
* **計算量 (Theoretical Cycles):** **10** ($100 \div 10$ ※モデル上の簡略化)
* **主な用途:** 画像処理、数値解析。

> **📚 根拠・出典**
>
> * **Intel® Intrinsics Guide (AVX2)**: `_mm256_add_ps` などの命令により、8つの単精度浮動小数点数を1サイクル（スループット基準）で処理する仕様。
> * **Flynn's Taxonomy (1966)**: コンピュータアーキテクチャの分類において、単一の命令ストリームで複数のデータストリームを処理する「SIMD」の定義。

---

## 3. GPU (Graphics Processing Unit)

**処理方式: SIMT (Single Instruction, Multiple Threads)**

NVIDIAなどが提唱するモデルです。数千のコア（CUDA Coreなど）が、それぞれ独立したスレッドとして同じ命令を異なるデータに対して実行します。メモリレイテンシを隠蔽するために、大量のスレッドを並列稼働させます。

* **視覚表現:** 「面（全体）」が一斉に点滅する。
* **実装ロジック:** Grid / Block / Thread 階層を用いた全要素に対する同時カーネル実行。
* **計算量 (Theoretical Cycles):** **1** (並列度内であれば)
* **主な用途:** AI学習（Training）、レンダリング。

> **📚 根拠・出典**
>
> * **NVIDIA CUDA C++ Programming Guide**: SIMTアーキテクチャの定義。Warp（32スレッドの束）単位での同時実行と、数千スレッドへのスケジューリングの仕組み。
> * **NVIDIA Ampere / Hopper Architecture Whitepapers**: ストリーミングマルチプロセッサ（SM）内でのFP32演算の並列性に関する記述。

---

## 4. TPU (Tensor Processing Unit)

**処理方式: シストリックアレイ (Systolic Array)**

Googleが開発したASICです。各演算ユニット（ALU）が格子状に並び、データが心臓の拍動（Systole）のように隣のユニットへ受け渡されながら、レジスタへの書き戻しを行わずに連続的に積和演算を行います。

* **視覚表現:** 「波」が斜めに流れていく。
* **実装ロジック:** データの再利用（Data Reuse）を最大化したパイプライン処理。
* **計算量 (Theoretical Cycles):** **10** (パイプライン段数・行列サイズ依存)
* **主な用途:** 大規模行列演算、AI推論・学習。

> **📚 根拠・出典**
>
> * **Jouppi, N. P., et al. (Google), "In-Datacenter Performance Analysis of a Tensor Processing Unit" (ISCA 2017)**: TPUのアーキテクチャを初めて詳細に公開した論文。シストリックアレイを用いることで、汎用CPU/GPUに比べてメモリアクセス頻度を劇的に減らし、ワットパフォーマンスを向上させるメカニズムが解説されています。

---

## 5. NPU (Neural Processing Unit)

**処理方式: タイルベース処理 / データフロー最適化 (Tile-based / Dataflow)**

エッジAI（スマホやIoT）向けに、限られたSRAM（キャッシュ）を効率的に使う設計です。大きな行列を「タイル」に分割し、オンチップメモリ内で処理を完結させます。また、値が0の計算をスキップする「スパース性（Sparsity）」や、重みの圧縮技術などが組み込まれます。

* **視覚表現:** 「ブロック（5x5の塊）」が順次処理される。
* **実装ロジック:** 空間的な局所性（Spatial Locality）を利用したブロッキング処理。
* **計算量 (Theoretical Cycles):** **4** (分割数依存)
* **主な用途:** モバイル端末でのAI推論（Inference）。

> **📚 根拠・出典**
>
> * **Sze, V., et al. (MIT), "Efficient Processing of Deep Neural Networks: A Tutorial and Survey" (Proceedings of the IEEE, 2017)**: AIアクセラレータ（NPU）における「Dataflow（データの流れ）」の重要性を説いた代表的な論文。エネルギー効率を高めるための「Tiling（タイリング）」や「Weight Stationary（重み固定）」といった手法が体系化されています。
> * **ARM Ethos-U NPU Technical Overview**: マイクロNPUにおけるMAC（積和演算）ユニットの構成と、SRAMへのアクセスを最適化するためのブロック処理に関する記述。

---

### 比較サマリ

| プロセッサ | 処理単位 | 実装概念 | 技術的出典 (Keyword) |
| :--- | :--- | :--- | :--- |
| **CPU** | Scalar | SISD, Sequential | Intel SDM (General Purpose) |
| **AVX2** | Vector | SIMD, Packing | Intel Intrinsics (YMM Registers) |
| **GPU** | Parallel | SIMT, Massive Threading | NVIDIA CUDA Guide (Warp/Grid) |
| **TPU** | Matrix | Systolic Array | Google ISCA 2017 Paper (Matrix Unit) |
| **NPU** | Block | Tiling, Dataflow | MIT "Efficient Processing" (Energy Efficiency) |
